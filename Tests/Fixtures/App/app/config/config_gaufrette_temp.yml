imports:
    - { resource: config.yml }

framework:
    test: ~
    session:
        storage_id: session.storage.filesystem

srio_rest_upload:
    temp_storage:
        type: gaufrette
        filesystem: gaufrette.temp_filesystem
    storages:
        default:
            accepted_mime_types: ['image/gif']
            type: gaufrette
            filesystem: gaufrette.test_filesystem