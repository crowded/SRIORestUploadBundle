imports:
    - { resource: config.yml }

framework:
    test: ~
    session:
        storage_id: session.storage.filesystem

srio_rest_upload:
    accepted_mime_types: ['image/gif']
    temp_storage:
        type: flysystem
        filesystem: oneup_flysystem.temp_filesystem
    storages:
        default:
            type: flysystem
            filesystem: oneup_flysystem.test_filesystem