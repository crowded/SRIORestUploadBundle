imports:
    - { resource: config.yml }

framework:
    test: ~
    session:
        storage_id: session.storage.filesystem

srio_rest_upload:
    temp_storage:
        type: flysystem
        filesystem: oneup_flysystem.test_filesystem
    storages:
        default:
            type: flysystem
            filesystem: oneup_flysystem.test_filesystem